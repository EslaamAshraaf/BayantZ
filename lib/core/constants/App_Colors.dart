import 'package:flutter/material.dart';

/// Purpose: Define all consistent color values used in the app.
/// These constants maintain visual consistency across themes.

class AppColors {
  // Light Theme Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color card = Color(0xFFFFFFFF);
  static const Color buttonBackground = Color(0xFFB5B4B4);

  // Status Colors
  static const Color present = Color(0xFF4BB609);
  static const Color absent = Color(0xFFDF1C1C);
  static const Color late = Color(0xFFFF814A);
  static const Color excused = Color(0xFF797979);

  // Table & Text Colors
  static const Color tableHeader = Color(0xFF2D2D2D);
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color Selectedtab = Color(0xFFE5B800);

  // Dark Theme Overrides
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);
  static const Color darkButtonBackground = Color(0xFF3A3A3A);
}
